# B 端模块更新日志

## 1.5.0

版本号与 A 端模块、b-app、服务端统一为 1.5.0。

- **KeyMint 连接失败会说明真实原因**：状态页能看到是系统拒绝了访问、服务没注册，还是设备只有旧版接口，不再统一显示成「服务找不到」。
- **实例名自动选择**：请求 `/default` 失败时，如果设备上恰好只注册了另一个非 StrongBox 的 KeyMint 实例，会自动改用它。StrongBox 请求不回退：缺 StrongBox 按失败处理，由 A 端降级。
