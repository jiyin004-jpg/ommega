# 三端源码复审报告

审计时间：2026-09-21 17:47
审计对象：工作区未提交改动（49 文件，+2551 / -696 行，只涉及 a-side 与 server）
审计方式：读码 + 编译/测试实测 + 权威定义对证 + 真机证书对拍；全程只读，未改动任何源码

---

## 一句话结论

这批修复的方向基本都对，主要的安全类修复（补丁级别 tag、EAT claim 编号、CBOR 解析边界、TLV 防回绕、空密码拒绝）我逐条验过，是真的修好了。但这次顺手加的 `hide_strongbox` 新功能带进来两个重的问题（一个能把 A 端 keystore 打崩并形成重启循环），模板脚本里还有一处路径拼错会让 PM 层隐藏直接失效，另外 CHANGELOG 里有一条声称是"已修"但实际没实现，还有归档目录的格式化没做干净会挡住提交。

所以：**不是全部正确，但也不算白改**。下面按「验过的」「有问题的」「还没验的」三档分开说。

---

## 一、我实际跑过的验证

| 项目 | 命令 / 方式 | 结果 |
|---|---|---|
| server 编译 | `cargo check --manifest-path server/source/Cargo.toml` | 通过 |
| server 测试 | `cargo test` | 14 项全过（1 ignored bench） |
| A 端 64 位编译 | `cargo check --target aarch64-linux-android --all-targets` | 通过 |
| A 端 64 位 lint | `cargo clippy --target aarch64-linux-android --all-targets -- -D warnings` | 0 warning |
| A 端 32 位 lint | `cargo clippy --target armv7-linux-androideabi --all-targets -- -D warnings` | 0 warning（CHANGELOG 关于 32 位 ABI `Into` 加宽的声称成立） |
| A 端 workspace 测试目标 | workspace 全量测试编译（含 ommega-injector、kmr-*、keymint、webui） | 全部编出可执行文件 |
| 真机证书对拍 | `OMMEGA_REAL_CERTS=D:\repos\keyattestation-lib\testdata cargo test real_device_certs` | 23 张全部通过 |
| EAT claim 编号 | 对 `D:\repos\KeyAttestation\...\EatClaim.java`（vvb2060 的实现，权威） | 全部对上，见下 |
| Magisk 挂载时序 | 拉 Magisk `native/src/core/module.rs:823-829` | 先 `setup_module_mount()` 后 `exec_module_scripts("post-fs-data")` |
| 代码级复现 | 抽取 filter.rs / attestation.rs 等价逻辑到独立文件跑边界 | 见下 |

---

## 二、确认正确的修复

这些都是我实测或对证过的，可以放心。

1. **EAT claim 编号全对**。拿 vvb2060 的 `EatClaim.java` 逐条对：`NONCE = -75008`、`SUBMODS = -76000`、`BOOT_STATE = -76003`、`PRIVATE_BASE = -80000`（KeyMint tag 用 `PRIVATE_BASE - tag`，正好对应 server 的 `-80_000 - 719`）、`NON_KM_BASE = PRIVATE_BASE - 2000 = -82000`，下面 `VERIFIED_BOOT_KEY = -82001`、`DEVICE_LOCKED = -82002`、`VERIFIED_BOOT_HASH = -82003`、`OFFICIAL_BUILD = -82006`。A 端和服务端取值一致。掩码也验了：`KEYMASTER_TAG_TYPE_MASK = 0x0FFFFFFF`，`0x0FFFFFFF & (KM_UINT|719) = 719`，所以 `-80719` 是对的。
2. **707/708 不再当补丁级别别名**。仓库自带 AOSP `Tag.aidl` 里 707 = `UNIQUE_ID`、708 = `ATTESTATION_CHALLENGE`，718/719 才是 `VENDOR`/`BOOT_PATCHLEVEL`。读路径只认 718/719、写路径也 push 718/719。
3. **扩展按内容嗅探**。DER `KeyDescription` 头字节恒为 `0x30`，CBOR map 是 `0xa0-0xbf`，两者不重叠，所以 `EAT_OID.or_else(ATTESTATION_OID)` + 首字节判断是可靠的，CBOR 挂在旧 OID 下也能解析。
4. **A 端 EAT/CBOR 解析**。抽等价代码跑了 9 项边界（含 `i64` 极值往返、深度上限 16、尾随数据拒绝、字节长度 `checked_add` 防回绕），全过。
5. **A 端远程 RSA 解密**。OAEP 优先、PKCS1 显式、`None` → NoPadding、其余报错，分支没错。
6. **filter.rs 全局作用域**。`!denied && (explicitly_in_scope || !system_like)` 让 `denied` 压过 scoop，与非全局路径口径对齐了。
7. **`take_tlv` 防回绕**。`pos.checked_add(len)` + 越界返回 None，畸形证书不再能触发切片 panic。CBOR 侧同样有 `try_from` + `checked_add` + `.get()`。
8. **notBefore 2050+ 用 GeneralizedTime**，阈值 `2_524_608_000_000` 正确。
9. **`record_token_use` 先 ROLLBACK 再还连接**，这个函数的修复是完整的。
10. **`verify_admin` 拒绝空密码**，主账号和 extra 条目都拒，fail-closed。
11. **pay 下单 30s 总超时**，且 pay.rs 全文只有这一处外呼，覆盖完整。
12. **autokeybox 禁用后可恢复**，线程只启动一次、toggle 只翻 `AtomicBool`，无死锁。
13. **B 端与 server 协议对齐**。`task_type` 三端都是 `"attest"/"sign"/"decrypt"`；端点 `/api/b/poll/`、`/api/b/result/`、字段名、StrongBox 拒答措辞全部对得上。b-side 和 b-app 三种任务都实现了。

---

## 三、有问题

### 高

**H1. `hide_strongbox` 打开后，GC 路径的 `unwrap()` 会 panic=abort，把 A 端 keystore 打崩并形成重启循环**

链路我逐段看过，都在 `a-side/source`：

- `src/keymaster/keymint_device.rs:913-916`：`KeyMintWrapper::new(STRONGBOX)` 新增了 `|| crate::config::strongbox_hidden()` 条件，命中就返回 `Err(ErrorCode::HARDWARE_TYPE_UNAVAILABLE)`。
- `src/global.rs:42-44`：GC 的失效闭包里是 `let km_dev = get_keymint_wrapper(security_level).unwrap();`，而 `get_keymint_wrapper` 就是直接调 `KeyMintWrapper::new`。强盒被隐藏时它返回 `Err`，`.unwrap()` 直接 panic。
- `Cargo.toml` 的 `[profile.release]` 里 `panic = 'abort'`。所以不是单线程崩，是**整个 keymint 进程 abort**。
- 触发路径：DB 里存在绑定在 StrongBox 上的 blob 且进入非 Current 状态 —— 应用删自己的 StrongBox key、`deleteAllKeys`/`clearNamespace`、key 升级 supersede、换 keybox 后退役旧绑定，都会走到 `src/keymaster/gc.rs:136` 的 `(self.invalidate_key)(uuid, &blob)`。
- 崩溃循环：`gc.rs:118-136` 是先把 blob id 推进 `deleted_blob_ids`（内存），调 `invalidate_key` 成功后才由 DB 层删行。进程 abort 时这个 id 没提交，下次启动 GC 按 `state != Current` 重新选中同一个 blob，再 abort。于是每次开机都崩一次，期间 A 端 keystore 全不可用。

修正方向：闭包里把 `HARDWARE_TYPE_UNAVAILABLE` 当成"这个 key 不用失效"处理（返回 `Ok(())`），或者在该分支提前判 `strongbox_hidden()` 直接跳过。

把握：**代码链路实测**（每一段都读过），**未实机复现**（需要设备上有旧 StrongBox 绑定 blob）。这是本次新增功能引入的，改之前不会有。

**H2. `hide_strongbox` 会改变 level-zero key 的选源，可能让既有 blob 全部解不开**

这条我读了实现，逻辑成立，但前提条件较窄，所以标存疑偏重。

- `src/keymaster/boot_key.rs:58-95`：`lookup_level_zero_km_and_strategy()` 读系统属性 `ro.keystore.boot_level_key.strategy`，**属性未设置就返回 `Ok(None)`**，然后走 infer。
- `boot_key.rs:107-122` infer 逻辑：TEE 版本 >= 4.1 → 选 TEE；否则 `KeyMintDevice::get_or_none(STRONGBOX)` 且版本 >= 4.1 → 选 StrongBox；再否则 TEE + `MaxUsesPerBoot`。
- `keymint_device.rs:212-219`：`get_or_none` 明确把 `HARDWARE_TYPE_UNAVAILABLE` 映射成 `Ok(None)`。也就是强盒被隐藏后，这里从"有 StrongBox"变成"没有"。

所以对一台 TEE < 4.1、StrongBox >= 4.1、且属性没被厂商固定的设备：原本 level-zero key 建在 StrongBox 上，打开 `hide_strongbox` 重启后 infer 改选 TEE，`boot_level_key` 换源 → super key 变化 → 既有 key blob 全部无法解密。

把握：**推断**。属性被 `PRODUCT_VENDOR_PROPERTIES` 固定过的设备不受影响。建议要么禁止在"已有 StrongBox 绑定 blob 的 DB"上翻这个开关，要么让 level-zero 的选源忽略 hide 标志。

### 中

**M1. 模板脚本给 `/system` 分区多拼了一层 `system/`，PM 层隐藏直接失效**

- `template/customize.sh:141-152`：`rel=${src_dir#/}`，对 `/system/etc/permissions` 得到 `system/etc/permissions`，再拼 `tgt="$MODPATH/system/$rel"` → `$MODPATH/system/system/etc/permissions/...`。模块里 `system/` 映射到 `/system/`，所以这个文件落到 `/system/system/...`（不存在），等于没隐藏。
- `template/post-fs-data.sh:115-132` 同一个毛病：`WL_RELS` 里含 `system/etc/permissions`，`marker="$MODDIR/system/$rel/$WL_NAME"` 同样多一层。有意思的是它上一行的存在性检测 `[ -f "/$rel/$WL_NAME" ]` 是**对的**（`/system/etc/permissions/...`），所以检测过、写入错，静默失效。
- 另外三项（`vendor`/`product`/`system_ext`）之所以碰巧对，是因为 root 管理器对 `system/<分区>` 这种 legacy 路径做了兼容。`/system` 没有这层兼容，必错。

修正：`/system` 这条用 `${src_dir#/system/}` 拿 `etc/permissions`，或者单独处理。

把握：**实测**（路径计算读出来就是错的）。

**M2. 关着开关装模块，装完第一次开机也会隐藏 StrongBox 声明**

- `template/customize.sh:141-159` 这段完全不读配置，只要设备上真实存在那个 feature XML 就无条件建 whiteout 标记。
- 而 `template/post-fs-data.sh:117-140` 才是按 `hide_strongbox` 增删标记的地方。
- Magisk 的顺序（我拉了源码 `native/src/core/module.rs:823-829`）：`setup_module_mount()` 先跑，`exec_module_scripts("post-fs-data")` 后跑。也就是说模块脚本里的 `rm -f "$marker"` 发生在挂载**之后**，只影响下一次开机。

所以默认（开关为 false）情况下：装完重启那一次，PM 报"没有 strongbox_keystore 声明"，而 daemon 因为 `hide_strongbox=false` 照样注册并服务 STRONGBOX —— 声明和行为自相矛盾。对一个专门对抗检测的模块来说，这本身就是个破绽指纹。对称地，用户关掉开关也要两次开机才恢复，跟 UI 上"重启后生效"的说法不符。

修正：`customize.sh` 里读一下 flat config 再决定要不要建标记；或者接受"打开开关需要两次重启"但把文案改对。

把握：**实测**（Magisk 时序来自源码，脚本内容读过）。KernelSU/APatch 的 overlayfs 时序可能不同，那两个管理器上要另外确认。

**M3. `queue.rs` 重置 `created_at_ms` 造成无上限重试**

- `server/source/src/queue.rs:367-370`：`reclaim_locked` 回收超时任务时把 `created_at_ms = now` 重置了（注释说"不重置的话刚回收的任务会立刻被判超时失败"，动机是好的）。
- 但 `expire_locked`（`queue.rs:281-284`）判 Pending 超时**只看 `created_at_ms`**，而 `pop_for_b` 每轮先 `reclaim_locked` 再 `expire_locked`（`queue.rs:217-219`）。
- 结果：只要一个任务反复被 B 端领取又超时（B 端崩溃、挂死，或者恶意 B 端只 poll 不 complete），TTL 时钟每轮被重置，永远不进 Failed。queue.rs 全文没有 attempts/retry 计数。
- 影响：A 端早在 `wait_result_timeout_secs`（默认 120s）就返回错误了，后面都是重复无效工作；任务不回收也不终结，admin 列表里它永远显示成"最新"。条目数不会增长（不新建任务），但也不会回收。
- 改之前 `pending_ttl`（默认 300s）能兜住这类任务，所以算本次引入的行为回归。

修正：加 attempts 计数，或者把"首次创建时间"和"上次重试时间"分成两个字段，expire 只看首次。

把握：**推断**（代码路径确定，未实机跑）。

**M4. CHANGELOG 声称"`android` 前缀不再误吞 `androidx.*`"没实现**

`a-side/source/ommega-injector/src/filter.rs` 的 `is_system_package_name` 用的是 `package == *prefix || package.starts_with(prefix)`，列表里同时有 `"android"` 和新增的 `"android."`。但 `"androidx.appcompat".starts_with("android")` 依然是 true。我把这段抽出来在本地跑过，`androidx.appcompat`、`androidx.core`、`androidauto.helper` 全部被判成 `system_like=true`。新增的 `"android."` 条目是冗余的，注释还自相矛盾。非全局路径用的是 `pkg == "android" || pkg.starts_with("android.")`（这个是对的），两处口径不一致。

把握：**实测**。

**M5. A 端 TA 不认 EAT 载荷，和 A 端 plat 层、server 层的口径不一致**

- `a-side/source/ta/src/cert.rs:288-293` 只匹配 OID `2.1.17` 且按 DER 解析，失败就 fail-open 返回 `None`。
- 而 `a-side/source/src/plat/attestation.rs`（本次新增）和 `server/source/src/cert.rs:1627-1636` 都按内容嗅探支持 EAT（`2.1.25`）。

后果：B 端如果输出 CBOR/EAT 链，走 A 端 TA 解析这条路会退回本地 ROT 值（`ta/src/keys.rs:227-245`）和本地 `attestationVersion`（`keys.rs:255-267`）。server 的注释里似乎以为 A 端也有同款处理，实际没有。

把握：**推断**（两侧实现差异是硬证据，没用真机 EAT 链验证过）。

### 低

**L1. 两个工作区的 `cargo fmt --check` 都不通过，会挡住提交**

用仓库自带钩子（`.cargo-husky/hooks/pre-commit`，对改动的 .rs 跑 `cargo fmt --check`）的精确命令跑，exit=1，位置：

- `server/source/src/cert.rs:689`
- `a-side/source/src/keymaster/boot_key.rs:303`
- `a-side/source/src/keymaster/db.rs:64`

巧的是这三处正好是本次审计修复新增的中文注释处。这会让钩子挡住提交，也推翻 CHANGELOG 里"fmt 清零"的说法。跑一下 `cargo fmt` 就完事。

把握：**实测**。

**L2. 陈旧注释和已修代码自相矛盾**

- `server/source/src/cert.rs:69` 还写着 `KM_TAG_VENDOR_PATCH_LEVEL (707) / KM_TAG_BOOT_PATCH_LEVEL (708)`。
- `server/source/src/fulfill.rs:333` 还写着 `per-partition patch levels (tags 707/708)`。

而同区间的代码已经改成 718/719 了。下次维护照着注释把别名加回来，刚修掉的"假补丁值"立刻复现。

把握：**实测**（读代码即可确认）。

**L3. notBefore ≥ 2050 且缺省 notAfter 会生成空有效期**

`server/source/src/cert.rs:682-704`：caller 给 `certificate_not_before_ms` ≥ 2050-01-01 又没给 `certificate_not_after_ms` 时，notBefore 走 GeneralizedTime 变成 2050+，而 notAfter 固定 `"480101000000Z"`（2048），于是 notBefore > notAfter。改之前 UTCTime 会把 `50...` 读成 1950，反而落在有效区间里 —— 所以这也是本次修复引入的回归，只是触发条件很偏（要调用方显式请求 2050+ 的 notBefore）。

把握：**推断**。建议加一句 `if not_after < not_before` 的夹取或报错。

**L4. `deliver_order_with_token` 里同类的"BEGIN 后提前返回不 ROLLBACK"还在**

`server/source/src/db.rs:684-687` 的 `SELECT ... FOR UPDATE` 出错时直接 `?` 返回，没回滚，带着未提交事务和可能已取得的行锁把连接还回池。`record_token_use` 修了，这个兄弟函数漏了。CHANGELOG 没声称修它，所以不算谎报。

把握：**推断**。

**L5. README 引用的发布包已被删，且文档里的产物名/目录/默认类型三处都不符**

- `b-side/build/ommegaclient-b-release-arm64-v8a-1.3.0.zip` 在工作区被删了（diff 里显示 `Bin 1777927 -> 0 bytes`），但 README 还在引用它。
- 更麻烦的是这个文件名不是脚本产物：`b-side/source/build.py` 默认出 **debug**（要 `--release` 才是 release），产物路径是 `b-side/source/target/ommegaclient-b-<type>-<abi>-<ver>-<git_hash>.zip`，跟 README 写的目录、类型、hash 后缀三处都不一样。a-side 同理。
- 顺带：两个 build.py 都不调用 `scripts/setup_cargo_config.py`，而它生成的 `.cargo/config.toml` 被 `.gitignore` 忽略，干净 clone 下直接 `python build.py` 会在链接阶段失败。

把握：读码确定，没实际跑构建。

**L6. `auth.rs` 密码比较不是恒定时间**

`server/source/src/auth.rs` 用的是普通字符串比较。管理端登录的计时侧信道，低危，但既然在改鉴权，顺手换成恒定时间比较更好。

---

## 四、存疑待验

1. **`hide_strongbox` 在 `KEYBOX_REFRESH_ENABLED=false` 时的入口问题**：`main.rs:119-127` 这种情况下根本不启动刷新线程，之后 admin 的 toggle 只置位，状态页会显示 `enabled=true` 但实际没有刷新。默认是 true，所以只在显式关掉 env 时出现。这恰好是 CHANGELOG 声称已消除的症状，只是入口换了个位置。
2. **EAT 尾随数据检查的位置和吞错**：`cert.rs:1327-1330` 的检查在字段填充之前，调用处（`cert.rs:1642-1643`）用 `let _ = eat::read(...)` 把错误吞了。CBOR map 后面多一个字节就整条 EAT claim 全丢且无日志。只是显示层丢字段，不是安全问题。
3. **Knox 空块显示**：`cert.rs:1542-1561` 对读不出任何字段的 Knox 扩展仍返回 `Some(default)`，前端只在 `!k` 时跳过整段，所以三星设备带畸形 Knox 块时会显示一整块破折号。
4. **status_ui.html 的 3 值标签**：`status_ui.html:171` 的 `fmtState` 给完整性字段（trust_boot/warranty/icd/kernel/system）回 3 时显示"未认证"，但 `cert.rs:1139` 明确写完整性是 `0 normal, 1 abnormal, 2 not supported`。同一个标签函数被两类字段共用，回 3 的样本我没拿到。
5. **admin 的 `public_status` 现在会吐出 Knox 字段**：`admin.rs:1139-1170` 把 `challenge`、`id_attest`、`record_hash` 加进了无鉴权端点。不含密钥材料，但等于任何人不用登录就能读到某设备最近一次认证的 Knox 记录哈希和挑战值。算不算敏感得你来定。

---

## 五、B 端既有状态

> 注：这一节写于当天早前的复审，那时 b-side / b-app 源码都没动。当天稍晚 b-app 的
> `KeystoreHelper.kt` 为对齐 StrongBox 拒绝原因做过改动（见第八节），b-side 仍为零改动。

`git status` 显示 b-side / b-app 源码本次**没有任何修改**（只有 `b-side/build/` 下那个 zip 被删）。所以下面这些是既有设计状态，不是这批改动引入的，列出来供参考：

- 协议层没问题：端点、字段、StrongBox 拒答措辞三端对齐。
- b-app 其实不是"配置界面"，它自己就是第二套 relay（用 Android Keystore API，不需要 root），配置存在 SharedPreferences 而不是 `/data/adb/ommega/relay.conf`。README 把它描述成"B 端管理界面"，和实现不符。同一台机器同时装模块和 APK 会产生两个 device_id、双注册。
- 时限阶梯倒挂：b-side 回传最坏耗时（4 次重试，约 139s）大于 server 的 `wait_result_timeout_secs`（120s）和 `assignment_timeout_secs`（60s）。抖动时会重复执行，而且晚到的结果不校验状态就覆盖已完成结果。
- 打包出来的模块没有看门狗（`template/service.sh:96-101` 只 `&` 启动一次），relay 退出就得等下次重启。
- b-app 对 StrongBox 请求静默降级成 TEE，和 b-side 明确报错的约定相反。
- b-app 丢弃 A 端请求里的 `attestation_application_id`（tag 709 固定成 `org.ommega.deviceb`）。

---

## 六、建议的动手顺序

1. 先修 H1（GC 那条 `unwrap`）和 M1/M2（模板脚本路径 + 无条件标记）。这三个是会让功能直接坏掉或者让机器变砖循环的。
2. 再修 M3（queue 无限重试）、H2（level-zero 选源）。
3. 跑一次 `cargo fmt`（两边），把 L1 清掉，别让钩子挡提交。
4. 把 CHANGELOG 里 M4 那条声称改掉，或者把 `is_system_package_name` 真正修成前缀边界匹配。
5. 其余低危项按需处理。

---

## 七、修复与验证（2026-09-21 当天完成）

上面 H1/H2/M1-M5/L1-L6 全部改完。改法、验证方式、真机实测结论如下。

### 改了什么

- **H1**（`a-side/source/src/keymaster/global.rs` + `keymint_device.rs`）：GC 闭包不再 `unwrap`。
  `get_keymint_wrapper` 返回的错误若是 `HARDWARE_TYPE_UNAVAILABLE`（就是 `hide_strongbox` 把 StrongBox
  藏了），当成"这个 key 不用失效"返回 `Ok(())`，DB 行不动、等下次开机再说；其它错误照常上抛。
  `uuid.to_security_level()` 拿不到级别也照样跳过。判定逻辑放在新增的
  `KeyMintDevice::is_hardware_type_unavailable(&anyhow::Error)`。
- **H2**（`keymint_device.rs` + `keymaster/boot_key.rs`）：`KeyMintWrapper::new` 拆成公开的 `new`
  和内部的 `new_inner(security_level, respect_hide)`；`KeyMintDevice::get` 拆成 `get_inner(level, true)`，
  新增 `get_probing_real_hardware()` / `get_or_none_probing_real_hardware()`。level-zero key 的选源探测
  走 `respect_hide = false` 那条路 —— 开关只影响对外声称，不影响自己找 key。
- **M1**（`template/customize.sh` + `template/post-fs-data.sh`）：`rel=${src_dir#/}` 之后再
  `rel=${rel#system/}`，`tgt` 保持 `"$MODPATH/system/$rel"`（`system_ext` 不会被 `system/` 前缀误伤）。
  `post-fs-data.sh` 的 `WL_RELS` 末项从 `system/etc/permissions` 改成 `etc/permissions`，并用
  `case "$rel"` 推出系统里的真实路径给存在性检测用（检测行和写入行现在同一口径）。
- **M2**（`template/customize.sh`）：whiteout 标记改成先读 `/data/misc/keystore/ommega/config` 里的
  `hide_strongbox|no_strongbox|hide_strongbox_keystore` 三个键，只有开了才建标记。副作用是安装完
  再打开开关要两次重启才生效（已接受，注释里写了）。
- **M3**（`server/source/src/queue.rs`）：`Task` 加 `pub attempts: u32`，`reclaim_locked` 每次回收
  `attempts += 1`，新增 `MAX_ASSIGN_ATTEMPTS = 5`，超了就判 `Failed` 并进 `failed_queue`，不再重排。
  这样毒任务最多被重试 5 轮就终结。
- **M4**（`a-side/source/ommega-injector/src/filter.rs`）：`is_system_package_name` 按前缀是否以
  `.` 结尾分两种匹配 —— 带点的走 `starts_with`，裸 `"android"` 走精确相等。`androidx.*` /
  `androidauto.*` 不再被判成系统包。
- **M5**（新的 `common/src/eat.rs` + `ta/src/cert.rs` + `src/plat/attestation.rs`）：
  - EAT/CBOR 解析器提到 `kmr-common::eat`（两边共用，不再各写一份），对外有
    `parse_claims` / `get` / `challenge` / `verified_boot_hash` / `root_of_trust` /
    `is_cbor_attestation_extension`，`Cbor` 枚举补上了 `Bool`（EAT 的 `DEVICE_LOCKED` 是布尔）；
  - `plat/attestation.rs` 里那份重复的 `mod eat` 删掉，改用共享的；
  - `ta/src/cert.rs` 找扩展时同时认 `2.1.17` 和 `2.1.25`，然后按载荷首字节分派：是 CBOR 就走
    `kmr_common::eat::root_of_trust`，否则照旧 DER 解析。EAT 里没有 DER 的 `verifiedBootState`，
    从 `DEVICE_LOCKED` 推（锁着 = Verified(0)，没锁 = Unverified(2)），`attestationVersion` /
    `keymasterVersion` 分别取 `-82004` / `-82005`。
- **L1**：两边 `cargo fmt --all`，`--check` 现在 exit 0。
- **L2**：`server/source/src/cert.rs` 和 `fulfill.rs` 里 707/708 的注释改成 718/719（并注明
  707/708 实际是 `UNIQUE_ID` / `ATTESTATION_CHALLENGE`）。
- **L3**：`server/source/src/cert.rs` 的 notAfter 改三分支 —— 先用 caller 给的，没给就用
  `notBefore + 1 天`，两者都没有才用 `DEFAULT_NOT_AFTER_MS`（2048-01-01）。notBefore > notAfter
  那个窗口没了。
- **L4**：`server/source/src/db.rs` 的 `deliver_order_with_token` 第一条 `conn.exec(...)` 改成仓库
  既有的 `.inspect_err(|_| { let _ = conn.query_drop("ROLLBACK"); })?` 写法。
- **L6**：`server/source/src/auth.rs` 加 `ct_eq_secret(a, b)` 做定长循环比较，替换掉两处 `==`。
- **L5**：README 里 A 端安装包版本号改成 1.4.1（实际产物是这个）；两个 `build.py` 新增
  `ensure_cargo_config()`，`.cargo/config.toml` 不在就自动调 `scripts/setup_cargo_config.py` 生成。

### 验证方式与结果

| 项目 | 怎么验的 | 结果 |
|---|---|---|
| M1/M2 脚本 | 丢到远程 Linux 主机（jiyin-server）跑 sh 端到端模拟，四个开关键组合 | 路径落点与标记增删全对 |
| M5 EAT 解析 | 把 `common/src/eat.rs` **原文件**放进最小 crate（只补一个同字段的 `RemoteRootOfTrust`），在远程 Linux 跑单测 | 10 项全过（ROT / 解锁态 / 缺 hash / 无 ROT claim / 挑战值 / boot hash / 尾随数据 / 嵌套深度 / 不定长 / 首字节判定） |
| 服务端 | `cargo test` | 14 passed / 0 failed / 1 ignored |
| A 端编译 | `cargo check --target aarch64-linux-android --all-targets` | 0 错 |
| A 端 lint | aarch64 与 armv7 两个 target 各跑 `cargo clippy --all-targets` | 0 clippy warning（只剩 `kmr-derive` 在 Windows 上生成 dylib 的链接提示） |
| 格式 | 两个工作区 `cargo fmt --all --check` | exit 0 |

（`kmr-common` 在 Windows host 上编译不过 —— 依赖链里的 `rsproperties 0.6.0` 用到了只有 Linux/Android
才有的 `rnet` API，所以 A 端的单测在本地跑不了，只能编译。这也是 M5 的单测放到远程 Linux 跑的原因。）

### 真机验证（nubia NX721J / Z60 Ultra，Android 16 / SDK 36，KernelSU）

设备这次连上了（`adb devices` 有 `320143945328 ... model:NX721J`，`su` 可用，context `u:r:ksu:s0`）。
用户说"设备应该没有 StrongBox"，**实测成立**，三方证据：

1. `/vendor/etc/vintf/manifest/android.hardware.security.keymint-service-qti.xml` 只声明
   `IKeyMintDevice/default`，`<version>3</version>`（KeyMint **3.0**），没有 strongbox 实例；
2. `/vendor/etc/permissions/` 下没有 `android.hardware.security.strongbox_keystore.xml`，
   `android.hardware.hardware_keystore.xml` 也只声明 `android.hardware.hardware_keystore version="300"`；
3. **ommega 自己的日志**（`/data/misc/keystore/ommega/logs/keymint.log`，2026-09-18 17:06:52）：

   ```
   keymint::plat::keymint_profile - optional StrongBox KeyMint HAL is not present:
       connect android.hardware.security.keymint.IKeyMintDevice/strongbox: NameNotFound
   keymint::keymaster::service - StrongBox KeyMint HAL is not present; skipping optional security level.
   ```

`/vendor/bin/hw/` 里确实躺着 `android.hardware.security.keymint-service-spu-qti`（高通 SPU，也就是它家
StrongBox 的载体），但既没进 VINTF 清单、进程也没跑（`ps -A` 只有 `...keymint-service-qti`），
属于"厂商打包了二进制但没启用"。

对上面结论的影响：

- **H2 在这台机器上不触发** —— 它的前提是"TEE < 4.1 **且** StrongBox >= 4.1"，本机 TEE 是 3.0 但
  StrongBox 根本不存在，`infer` 里那条分支走不到。修它是为了别的机型（有 StrongBox 的高通/三星机）。
- **H1 在这台机器上也难触发** —— 没有 StrongBox 就不会有 StrongBox 绑定的 blob。但 `hide_strongbox`
  开关本身在无 StrongBox 机型上就是空操作，逻辑正确性仍得保证（GC 那条路是通用的 unwrap）。
- **M1/M2 在这台机器上是无害跳过** —— 找不到 strongbox 声明文件就不建标记，bug 只在真声明了
  StrongBox 的机型上显现。

设备当前状态（供对照）：模块 `ommega 1.4.1-ec3df76`（versionCode 22，**不含本轮修复**），
config 里 `remote: true`、`global_scope: true`、`tls_insecure: true`，`hide_strongbox` 未开，
`crash_count` 为 0，守护进程正常在跑（watchdog 每 5 分钟 `idle -> terminating` 是设计行为）。

### 没动的

- 存疑项 1-5（`KEYBOX_REFRESH_ENABLED=false` 时的状态页、EAT 尾随数据的吞错位置、Knox 空块显示、
  `status_ui.html` 的 3 值标签、`public_status` 吐 Knox 字段）—— 显示层/边缘问题，且第 4 条没拿到
  回 3 的样本，先没动，等你定。
- 第五节那批 B 端既有状态 —— b-side / b-app 源码依然零改动，不在范围内。

## 八、智能模式（Smart）StrongBox 通路复审（2026-09-21）

### 你问的两个场景

**场景一：A 端要 StrongBox，B 端有 StrongBox 但没预制密钥 —— 原生错误能不能逐级回传到调用方？**

能，链路我逐层对过：

- **B 端（b-side 二进制 relay）**：`tee_ops.rs:347-349` 给 generateKey 的失败串拼上 `[km_error=-74]` / `[km_error=-68]`，
  `relay.rs:644-661` 认这两个码，产出固定文本 `HAL exists but attestation keys not provisioned (factory provisioning issue)` /
  `HAL exists but hardware type unavailable`，包成 `{"error": "strongbox not supported: <reason>"}` 上报。
- **服务端**：`handlers.rs:288-297` 的 `strongbox_b_kind` 子串匹配这两句话 → `strongbox_unprovisioned` / `strongbox_unavailable`，
  Smart 模式里带 HTTP 200 加 `relay_error_kind` 标记返回（注释写明了：A 端只看 2xx 的 body，标记必须骑在成功状态码上）。
- **A 端**：`remote.rs:473-490` 读到 `relay_error_kind` 后映射成真 KeyMint 错误 —— `strongbox_unprovisioned` →
  `AttestationKeysNotProvisioned`(-74)，`strongbox_unavailable` → `HardwareTypeUnavailable`(-68)，其它 → `UnknownError`，直接 `return Err`。
- **TA 上层不吞**：`ta/src/keys.rs:424` 用的是 `self.generate_key_remote(...)?`，`?` 把 Err 直接扔给调用方；只有 `Ok(None)`
  （远端不可达）才落回本地 keybox。所以调用方能按 AOSP 惯例区分 —— -68 会被 framework 转成 `StrongBoxUnavailableException`，
  -74 是带错误码的 `KeyStoreException`。

唯一前提：服务端 keybox 那层没先成交。Smart 是「服务端 keybox 优先」，只要服务端存了这台设备的身份，第一步就出证了，
压根不会去问 B 端，也就不会有这个错误。

**场景二：A 端要 StrongBox，B 端不支持 —— 能不能逐级回退到服务端密钥层？**

能，两条 relay 路径我都看了：

- **b-side 二进制**：连不上 HAL 时报 `StrongBox HAL service not present on this device`，`strongbox_b_kind` 不认这句
  （它不是「有 HAL 但坏了」），于是继续往下走，服务端 keybox 出场。
- **b-app（Android Keystore API 那套）**：这条路径不报错 —— `setIsStrongBoxBacked` 在无 StrongBox 设备上的官方行为
  就是静默降级 TEE，链如实标 `TRUSTED_ENVIRONMENT`。

### 这一路查出来一个真问题

Smart 模式的 B 端分支原来只判「有没有 error 字段」，没看链本身是什么级别：

```rust
if v.get("error").is_none() && !attest_chain_empty(task_type, &v) {
    // 直接当成「B 端真 StrongBox 成交」返回
    return Json(v).into_response();
}
```

b-app 那条路径正好钻这个空子 —— 它返回一条**诚实标着 TEE** 的链，服务端却认为「B 端成交」，把 TEE 链直接给了 A 端。
结果：调用方请求 StrongBox，拿到的认证是 TEE；而服务端 keybox 明明能出 StrongBox 标记的链，没轮到。这跟 `strongbox.rs`
文档里写的「B 端真 StrongBox 成功就原样返回」对不上 —— b-app 走的不是 StrongBox HAL，是 Keystore API 的静默降级。

**修法**（`server/source/src/cert.rs` + `handlers.rs`）：B 端分支加一道安全级别校验，只有链叶子确实是 StrongBox 才算成交。

- ASN.1 那份：`KeyDescription` 的第 2 个字段就是 `attestationSecurityLevel`（裸 ENUMERATED），`read_asn1_attestation`
  以前循环 6 次把它跳掉了，现在留下来了。
- EAT 那份：按 `EatClaim.SECURITY_LEVEL`(-76002) 的 1/3/4 映射成 KeyMint 的 0/1/2（依据是 `D:\repos\KeyAttestation` 的
  `EatAttestation.eatSecurityLevelToKeymintSecurityLevel`，实锤）。
- 判据是 `Some(2)`；TEE 链和「读不出来的链」（`None`）一视同仁，继续走服务端 keybox → A 端本地 keybox 回退。
  宁可多走一步拿到正经 StrongBox 链，也不把来路不明的链放过去。
- `DeviceBootInfo::security_level` 刻意没进 `is_empty()`：安全级别跟启动状态没关系，进了的话「只有安全级别的链」
  会在状态页多冒出一个空启动块（跟存疑项 3 同类的毛病）。

### 验证

- `cargo test`（server）：18 passed / 0 failed / 1 ignored（原来 14，新增 4 —— ASN.1 级别直读、EAT 映射、未知级别留空、坏链返 `None`）
- `cargo fmt --all --check` exit 0；`cargo clippy --all-targets` exit 0
- A 端这轮没动代码：Smart 策略是服务端的事，A 端只要不是 `relay_error_kind` 就照旧 `Ok(None)` / `Ok(Some(chain))`，行为不变

### b-app 对齐（2026-09-21 当天追加）

上面那条不一致已经对齐了。查证过程：`android.security.KeyStoreException` 的 `getErrorCode()` 在 AOSP 里是
`@hide` + `@TestApi` 的 **public** 方法（`keystore/java/android/security/KeyStoreException.java`，正则从
`android.googlesource.com` 拉的），返回未经裁剪的 `mErrorCode` —— 对 KeyMint 层失败就是 -68 / -74，正是要的东西；
而公开的 `getNumericErrorCode()` 会把 -68 粗化成 `KEYMINT_UNIMPLEMENTED_ERROR`(12)，不够用。另外
`android.security.keystore` 包里确实有个专门的 `StrongBoxUnavailableException`，所以两条路都得认。

改动全在 `b-app/.../KeystoreHelper.kt`（这个文件此前一直是零改动）：

- 新增 `strongboxRefusalReason(e)`：沿 cause 链最多 8 层，碰到 `StrongBoxUnavailableException` 直接归
  `hardware type unavailable`；碰到 `KeyStoreException` 就反射 `getErrorCode()`，-74 / -68 各自对应。
- 新增 `declaresStrongBox(context)`：`PackageManager.hasSystemFeature("android.hardware.strongbox_keystore")`，
  用字符串常量，不需要 API 守卫。
- `attest()` 生成后：`isStrongBoxBacked(alias) == false && declaresStrongBox(context)` → 报
  `strongbox not supported: HAL exists but hardware type unavailable`。设备没声明 StrongBox 时不报，
  照旧放行 TEE 链（与真实设备一致，那条链由服务端的级别校验兜住）。
- `attest()` 的 catch：StrongBox 请求才做映射，映射不出就退回原始 `e.message`。
- `verifyStrongBoxBacked` → `isStrongBoxBacked`，返回 `Boolean?`：API < 31 没法判断实际落在哪层时回 `null`。
  原先固定回 `false`，会让一台**真有** StrongBox 的 Android 12 以下设备被谎报成「HAL 不可用」——这个
  修正本身就是一个小 bug fix。

四种情况现在都覆盖：真有 StrongBox 正常出 StrongBox 链；HAL 抛 -74 → `attestation keys not provisioned`；
HAL 抛 -68 或静默降级 → `hardware type unavailable`；设备本来就没 StrongBox → 静默降级 TEE。前三种
server 的 `strongbox_b_kind` 都认得，与 b-side 完全等价。

**已编译验证**（2026-09-21 当天补的）。本机原本没 SDK，当天把工具链整到 D 盘后跑通了：
`D:\android-sdk`（commandline-tools 12.0 + `platforms;android-36` + `build-tools;36.0.0`，
AGP 又自己补了 35.0.0）+ `D:\gradle-home`（Gradle 8.13 + 依赖缓存），JDK 用 Android Studio 自带的
jbr 21（项目 `jvmToolchain(21)` 正好）。依赖源在 `GRADLE_USER_HOME/init.d/mirror.gradle` 里换成了阿里云：
`repo.maven.apache.org` / `plugins.gradle.org` 这边直连返 403，试过阿里云三个仓库全 200。
这个 init 脚本不进仓，只影响本机。

结果：`:app:compileDebugKotlin` 与 `:app:assembleDebug` 两份 BUILD SUCCESSFUL（首次 9m 4s / 二次 26s），
Kotlin 编译器零警告（日志里唯一的 WARNING 是仓库自己设的 `android.overridePathCheck` 实验选项）。
用 javap 查了编译产物坐实：`isStrongBoxBacked` 的返回类型是 `java.lang.Boolean`（装箱，即源码里的
`Boolean?`）、`strongboxRefusalReason(Throwable)` / `declaresStrongBox(Context)` 两个方法都在，
字节码常量池里能直接看到那三句话和 `android.hardware.strongbox_keystore`。

剩下仍未对齐的一处（故意不动的）：b-side 遇到「HAL 在但拒绝生成（参数/版本不匹配）」时是
`HAL rejected key generation (possible parameter/version mismatch)`，server 不认这句 → 走到回退而不是
surface。这里选择保持回退 —— 参数不匹配往往是 ommega 自己发的参数组合 HAL 不吃，surface 出去会让
所有这类请求都硬失败，而回退到服务端 keybox 反而能出证。

### 已修：A 端对非 2xx 的处理（回退链最后一环）

顺手确认过：`remote.rs` 的 `post_json` 文档写明「`Ok(Some(json))` on 2xx with a JSON body,
`Ok(None)` if the remote is unreachable/non-2xx」，所以 Smart 模式第 4 步那个 500 会让 A 端拿到
`Ok(None)` → 落回本地软件 keybox 出 StrongBox 链。回退链最后一环是通的。

---

## 九、B 端启动崩溃的根因（2026-09-21 当天追加，真机实测）

用户报「b 端启动不起来」。查下来是**编译配置的锅**，不是业务代码问题。

### 现象

`module.prop` 卡在「⏳ 启动中」，relay 进程起不来。翻 `/data/tombstones/` 发现每次启动都留一份
SIGSEGV：`signal 11, cause: null pointer dereference, fault addr 0x0`，`Process uptime: 0s`
—— 进程刚起就死，日志一行都没写。

### 排查

- relay.log 停在换模块之前那一刻，之后没有任何输出
- tombstone 里 `pac_enabled_keys: 0xf`（PAC 全键启用），unwinder 解不开栈，backtrace 只有 2 帧且落在
  libc 内部，地址符号化出来是 `getaddrinfo`/`page_size`，明显不对，不可信
- `LD_LIBRARY_PATH` 清空跑、带 `/vendor/lib64` 跑，**两种都崩** → 排除环境变量
- 把 relay 挪到 `/data/local/tmp` 跑，照样崩 → 排除模块目录路径与 SELinux

### 根因

从 git 把上一版能跑的 relay（`HEAD:b-side/build/...zip`，3497304 字节）导出来对比动态依赖：

| | NEEDED |
|---|---|
| 上一版（能跑） | `liblog.so, libdl.so, libm.so, libc.so` |
| 本轮重编（崩） | `liblog.so` ← libc/libdl/libm 全静态进去了 |

**b-side 的 `scripts/setup_cargo_config.py` 生成的 rustflags 里多了一个
`-L native=<NDK>/sysroot/usr/lib/aarch64-linux-android`。**

NDK 那个目录里**只有静态库**（`libc.a` 6.7MB、`libm.a`、`libdl.a`…），版本化的 `.so` stub 全在
`21/`~`35/` 这些 API 子目录里，而且本来就在 clang 默认搜索路径上。`-L` 把它排到前面之后，linker
找 `-lc` 先命中 `libc.a`，于是把 bionic 静态链了进去。

静态 bionic 在 Android 上是不支持的（TLS、`getauxval`，以及一堆依赖 linker 提供的机制都撑不住），
所以 relay 一走到 `rsbinder::ProcessState::init_default()` 就空指针崩。

### 为什么之前没事

这个 `.cargo/config.toml` 是 `build.py` 新加的 `ensure_cargo_config()` **本轮首次自动生成**的
（`**/.cargo/` 在 .gitignore 里，从不入库）。上一版是手工用环境变量传 linker 编的，没走这个脚本。
而 a-side 的同名脚本**没有**这行 `-L`，所以 a-side 编出来的 `keymint` / `ommega-inject` 依赖都正常，
一直没暴露 —— 这两份脚本是各自仓库里的独立文件，不是共享的。

### 修法

`b-side/source/scripts/setup_cargo_config.py` 两处 rustflags 去掉 `-L native=...`，只留
`["-C", "link-arg=-lc++abi"]`（与 a-side 一致），顺带删掉因此变成死代码的 `toml_escape()`。

### 验证

- 重编后 `NEEDED = liblog.so, libdl.so, libm.so, libc.so`，与上一版一致；体积从 3.75MB 回到
  **3.38MB**（上一版 3.34MB）
- 推真机跑：进程活着，日志完整 —— `file logging enabled` → `Binder driver protocol version: 8` →
  `relay daemon starting (config from file) server=http://110.40.170.96:10886 device=device-b-c3f204aa`
- 真有业务：`b/poll status=200` → 接连领到 sign 任务 → `b/result ... accepted (HTTP 200)`，与服务器通信全通
- 重跑 `build.py --release --abi arm64-v8a` 打包，zip 里 relay 的大小和 sha256 与本机产物一致
- 已把修复版覆盖进设备模块目录（sha256 `1bace309...` 与本机完全一致），用户重启后由模块正常接管

### 顺带发现

- relay 启动时 `load_all_sessions()` 会载入 **19967 个**持久化 session（`/data/adb/ommega/sessions`，
  162MB），耗时 12 秒。不致命，但明显偏慢，长期得清或者加个上限。
- 设备上 KernelSU 的 `su` 上下文（`u:r:ksu:s0`）在 Android 16 + realme/OPPO 的 secureguard 下读不了
  `system_file` label 的模块文件（`cat module.prop` 直接 Permission denied），所以 adb 侧没法直接核对
  模块文件内容，只能靠 sha256 比对。

---

## 十、单包多 ABI 与 WebUI 复选框（2026-09-21 追加）

两个反馈：① A 端 WebUI 远程配置里的复选框被长文案挤得只剩一半；② A/B 端模块不该按平台出一堆包，
应该一个包装全部 ABI、安装时再选。

### ① 复选框被挤扁

WebUI 前端源码不在仓库里（只有 Vite 构建产物和 `template/webroot/index.html` 这份手写文件），
问题就在这个 HTML 里。「远程配置」对话框的 5 个选项都长这样：

```html
<label class="config-option" style="display:flex;align-items:center;gap:4px">
  <md-checkbox id="remote-config-hide-strongbox"></md-checkbox>
  <span data-i18n="remote_config_hide_strongbox"></span>
</label>
```

`md-checkbox` 是 Material Web 自定义元素，在 flex 容器里默认 `flex-shrink:1`；而
`remote_config_hide_strongbox` 的文案（「声明不支持安全模块 StrongBox（应用将视为设备没有安全模块；
重启后生效）」）长到 30 多字，一行放不下就把勾选框压成一半。

修法：`align-items` 改 `flex-start`（多行时勾选框贴第一行）、`gap` 加到 8px、勾选框加
`flex-shrink:0;margin-top:2px`、文字加 `flex:1;min-width:0`。文案同时压成
「声明不支持 StrongBox（重启后生效）」，中简 / 中繁 / 英三份 locale 同步改。

### ② 单包多 ABI

翻代码发现设计意图本来就是单包：A 端 `customize.sh` 开头就写 `SUPPORTED_ABIS` 把全部 ABI 列出来，
后面按 `$ARCH` 提取对应 `libs/<abi>/`；B 端 `customize.sh` 的 `case "$ARCH"` 里同时检查
arm64-v8a 和 x86_64；仓库里入库的 `ommega-a-release-1.4.1.zip` 本身就是 4 ABI 合并包。真正跟这套
设计拧着来的是 `build.py` —— 它按 `--abi` 每个 ABI 出一个包，跟入库包对不上。

改法（两端 `build.py` 同构）：新增 `build_combined_package()` 作为默认路径 —— 把受支持 ABI 逐个编好，
二进制分别放进同一个 stage 的 `libs/<abi>/`，模板只复制一次（`SUPPORTED_ABIS` 写全部 ABI），
sha256 和打包各做一次，产出一个不带 ABI 后缀的 zip。原来按 ABI 出包的路径挪到 `--split` 后面。

A 端有个坑：`copy_template_files(stage_dir, abi)` 本来会用 `TEMPLATE_ABI_EXCLUDES` 给非 arm64 的
ABI 排除 `pathmask/`（那 7 个内核模块只有 arm64 构建）；单包时不能排除，所以 `abi=None` 时排除集
为空。`customize.sh` 里也只有 arm64 分支才 extract pathmask，两边一致。

### 顺手挖出来的真 bug：运行时按目录顺序选二进制

改完单包才反应过来：既然同一个包里会同时带多个 ABI 的二进制，运行时靠什么选？

- A 端 `template/daemon` 的 `find_module_keymint()`：`$MODDIR/keymint` → `libs/arm64-v8a/keymint`
  → `libs/x86_64/keymint`，纯顺序 fallback。
- A 端 `template/daemon-injector` 的 `find_module_injector()`：同一个毛病。
- B 端 `template/service.sh` 的 `find_module_relay()`：同一个毛病。

单 ABI 包时只有一条路，看不出来；多 ABI 包里 x86_64 设备会命中排在前面的 arm64 二进制。之前没
暴露纯粹因为 x86_64 设备太少。三处都加了 `device_abi()`：先用 `getprop ro.product.cpu.abi`，拿不到
退回 `uname -m`，映射到 `arm64-v8a / armeabi-v7a / x86 / x86_64` 后取 `libs/<abi>/`；都解析不出来
才退化成随便挑一个存在的。A 端因为 `customize.sh` 有 `SKIPUNZIP=1` 且只 extract 本机 ABI 的目录，
实际踩不到；B 端没有 `SKIPUNZIP`，Magisk 会把两个 ABI 全解出来，所以 B 端是实打实会踩的。

### 验证

- B 端 `python build.py --release` → `ommegaclient-b-release-1.3.0-feda12e.zip`，3.5MB，含
  `libs/arm64-v8a/relay` + `libs/x86_64/relay`，两个二进制的 `NEEDED` 都是 `liblog/libdl/libm/libc`
- A 端 `python build.py --release` → `ommega-a-release-1.4.1-feda12e.zip`，23.12MB，8 个二进制
  （4 ABI × keymint/ommega-inject）全在；`SUPPORTED_ABIS="arm64 arm64-v8a arm armeabi-v7a x86 x64 x86_64"`；
  包内 `module.prop` 是 `version=1.4.1-feda12e`、`versionCode=23`
- 三个改过的 shell 脚本（`daemon`、`daemon-injector`、`service.sh`）推到真机上过 `sh -n`，全 OK
- 包内 `customize.sh` / `daemon` / `daemon-injector` 逐个读出来核对（`device_abi` 都在、
  `SUPPORTED_ABIS` 是全部 ABI）
- 入库包已同步：`a-side/build/ommega-a-release-1.4.1.zip`（23.12MB）、
  `b-side/build/ommegaclient-b-release-1.3.0.zip`（3.5MB，文件名去掉了 ABI 后缀）

**把握**：构建、包内容、语法都是实测。「x86_64 设备会拿到 arm64 二进制」是读码推断，手上没有 x86_64
真机可验；但 B 端模块目录里确实会同时存在两个 ABI（没有 `SKIPUNZIP`），选择逻辑也确实只是顺序
fallback，这条推断的链条是完整的。

> 后续发版把版本号提到 A 端 **1.4.2**、B 端模块 **1.3.1**、b-app **1.3.1**，入库包名随之变为
> `ommega-a-release-1.4.2.zip` 和 `ommegaclient-b-release-1.3.1.zip`（上文本节里写的 1.4.1 / 1.3.0
> 是升版前那一刻的实测值）。
