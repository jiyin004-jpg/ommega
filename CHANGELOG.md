# A 端更新日志

## 1.4.4

（A 端模块 1.4.4，versionCode 32；本版模块本身没有改动，改动都在服务端）

服务端：

- **B 端一连上服务端就自动做一次自检**，把设备 TEE 状态（启动信息）落到状态页，不用再等它恰好接到一次
  A 端请求。服务端重启后的那段空白、以及从来没接到过请求的设备（比如 TEE 出问题的）以前在页面上是
  一片空白，现在会直接显示自检失败的原因。`RELAY_B_SELFCHECK=0` 可以关掉。
- 逐层回退的日志文案修正：`empty cert chain from B device` 以前是写死的，keybox 层失败也这么打，把人
  往 B 端引；现在按层区分并带上设备 ID。
- 状态页会显示链里的 **AAID（KeyMint tag 709）**：设备自己的 app 出证写的是 `org.ommega.deviceb`，
  模块替请求方出证写的是请求方的包名（如 `com.android.vending@85322340`），一眼能看出一条链是谁的身
  份出的。取的是设备自己上报的最后一条链，链失败时保留上一次的值。
- **等 B 设备结果的上限从 180s 收到 3s**（认领死线 60s → 3s）：一次 TEE 调用是百毫秒量级，设备又在
  长轮询上等着拿任务，等几十秒只造成「A 端 30s 就放弃、服务端还在傻等」，这期间用户手里什么都没有。
  现在 3s 内没结果就快速失败、交给回退阶梯（服务端密钥 → 自签 → A 端本地），A 端总能拿到一条能用的
  链。可用 `RELAY_WAIT_RESULT_TIMEOUT` / `RELAY_ASSIGNMENT_TIMEOUT` 调。
